import { CustomThemeDirective } from './custom-theme.directive';

describe('CustomThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomThemeDirective();
    expect(directive).toBeTruthy();
  });
});
